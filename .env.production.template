# TechDukaan Frontend Production Environment Configuration Template
# Copy this file to .env.production and fill in your actual values
# 
# 🔒 SECURITY: Never commit .env.production files with real values!
# This template is safe to commit and helps team members set up their environment.

# =============================================================================
# 📝 SETUP INSTRUCTIONS
# =============================================================================
# 1. Copy this file: cp .env.production.template .env.production
# 2. Replace all placeholder values with your actual values
# 3. Deploy to Vercel with these environment variables
# =============================================================================

# Supabase Configuration (✅ SAFE for client-side use)
# Get these from: https://supabase.com/dashboard/project/[your-project]/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://[your-project-id].supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.[your-supabase-anon-key]

# App Configuration (✅ SAFE - public information)
NEXT_PUBLIC_APP_URL=https://your-domain.com

# MeiliSearch Configuration (✅ SAFE - public URLs only)
# Your MeiliSearch instance URLs
NEXT_PUBLIC_MEILISEARCH_HOST=https://search.your-domain.com
NEXT_PUBLIC_MEILI_URL=https://search.your-domain.com

# MeiliSearch API Key (✅ SAFE - search-only key for client-side use)
# ⚠️  IMPORTANT: Use a search-only API key, NOT your master key!
# 
# 🔧 HOW TO GENERATE MEILISEARCH PUBLIC SEARCH-ONLY API KEY:
# 1. SSH into your server where MeiliSearch is running
# 2. Run this command (replace YOUR_MASTER_KEY_HERE with your actual master key):
#
# curl -X POST 'https://search.your-domain.com/keys' \
# -H 'Authorization: Bearer YOUR_MASTER_KEY_HERE' \
# -H 'Content-Type: application/json' \
# --data-binary '{
#   "description": "Frontend Search Key",
#   "actions": ["search"],
#   "indexes": ["*"],
#   "expiresAt": null
# }'
#
# 3. Copy the returned "key" value and paste it below
# 4. This key is SAFE for client-side use (only allows search operations)
NEXT_PUBLIC_MEILISEARCH_API_KEY=your-search-only-api-key-here

# Medusa Backend Configuration (✅ SAFE - public URL and publishable key)
NEXT_PUBLIC_MEDUSA_BACKEND_URL=https://api.your-domain.com
# Get publishable key from Medusa admin: Settings > Publishable API Keys
NEXT_PUBLIC_PUBLISHABLE_API_KEY=pk_[your-medusa-publishable-key]

# Site Information (✅ SAFE - public information)
NEXT_PUBLIC_SITE_NAME=Your Site Name
NEXT_PUBLIC_COMPANY_NAME=Your Company Name
LIVE_URL=https://your-domain.com

# Feature Flags (✅ SAFE - configuration only)
IS_VERCEL_ANALYTICS_ENABLED=false
IS_GTM_ENABLED=false

# Contact Information (✅ SAFE - public contact information)
NEXT_PUBLIC_WHATSAPP_NUMBER=+1234567890

# =============================================================================
# 🔒 SECURITY NOTES
# =============================================================================
# 
# SAFE TO EXPOSE (NEXT_PUBLIC_ prefix):
# - Supabase URL and anonymous key (designed for client-side use)
# - Public URLs and publishable API keys
# - Site information and feature flags
# 
# NEVER EXPOSE:
# - Database passwords or connection strings
# - Private API keys or secrets
# - Master keys or admin tokens
# 
# For MeiliSearch:
# - ✅ Use search-only API keys for client-side
# - ❌ Never expose master keys or admin keys
# 
# =============================================================================
